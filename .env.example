# 必填：控制台管理员（单用户；首次启动建号后改口令请走数据库）
ADMIN_USERNAME=wannayoung
ADMIN_PASSWORD=52111314

# Telegram API（可选；不填则启动时自动用 Desktop 公开凭证 2040）
TG_APP_ID=
TG_APP_HASH=

# 可选（本地开发覆盖 Docker 默认值）
BIND=0.0.0.0:3000
WEB_DIR=./web/dist
CONFIG_PATH=./config/config.yaml
# JWT_SECRET=
